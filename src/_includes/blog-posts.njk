<ul class="blog-post-list">
  {%- for post in collections.posts | reverse -%}
    <li>
      <span><code>{{ post.date | dateFormat }}</code></span>
      <span><a href="{{ post.url }}">{{ post.data.title }}</a></span>
      <p>{{ post.data.description }}</p>
    </li>
  {%- endfor -%}
</ul>
